"use strict"; //treat all JS code as newer version
//alert(3+3)//0p-error because  we are using nodejs, not browser

console.log(3 + 3); //code readability should be high

//data types-
//number -> 2 to power 53
//bigint
//string -> ""
//boolean -> true/false
//null -> standalone value
//undefined ->
//symbol

//typeof-
// used to determine the data type of a value. It returns a string representing the type of the operand

console.log(typeof undefined); //op-undefined (js bug)
console.log(typeof null); //op-object (js bug)
console.log(typeof String);
